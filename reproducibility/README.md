# Reproducibility Records

- `v1_split_manifest.json`: V1 prompt split manifest.
- `v2_dataset_manifest.json`: V2 source revisions, counts, hashes, filtering, and overlap audit.
- `data_split_audit.md`: independent exact-overlap audit.

Use the manifests to reconstruct data locally. Raw examples are intentionally excluded because of source-license and redistribution constraints.
